<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.douban.mapper.PersonMapper">
    <select id="findPersonById" resultType="com.example.douban.pojo.Person">
        SELECT * FROM persons WHERE personID = #{id};
    </select>
    <select id="findPersonByMovie" resultType="com.example.douban.pojo.Person">
        SELECT personID, name, img, sex, birthday, birthplace, summary, role
        FROM persons, relationship
        WHERE relationship.person_id = persons.personID
        AND relationship.movie_id = #{id}
        ORDER BY
        CASE
          WHEN role = '演员' THEN 3
          WHEN role = '导演' THEN 1
          WHEN role = '编剧' THEN 2
        END
        LIMIT ${limit} OFFSET ${offset};
    </select>
    <select id="countPersonByMovie" resultType="int">
        SELECT count(*)
        FROM persons, relationship
        WHERE relationship.person_id = persons.personID
        AND relationship.movie_id = #{id}
        ORDER BY
        CASE
          WHEN role = '演员' THEN 3
          WHEN role = '导演' THEN 1
          WHEN role = '编剧' THEN 2
        END;
    </select>
    <select id="findMovieByPerson" resultType="com.example.douban.pojo.Movie">
        SELECT DISTINCT movieID,
               name,
               director,
               actor,
               genre,
               tag,
               summary,
               rate / 2 AS rate,
               popular,
               year,
               ratingsum,
               img,
               tags,
               region
        FROM movie, relationship
        WHERE relationship.movie_id = movie.movieID
        AND relationship.person_id = #{id}
        ORDER BY popular DESC
        LIMIT ${limit} OFFSET ${offset};
    </select>
    <select id="countMovieByPerson" resultType="int">
        SELECT count(DISTINCT movie.movieID)
        FROM movie, relationship
        WHERE relationship.movie_id = movie.movieID
        AND relationship.person_id = #{id}
        ORDER BY popular DESC;
    </select>
    <select id="findPersonByKeywords" resultType="com.example.douban.pojo.Person">
        SELECT personID, name, img, sex, birthday, birthplace, summary
        FROM persons
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
        LIMIT ${limit} OFFSET ${offset};
    </select>
    <select id="countPersonByKeywords" resultType="int">
        SELECT count(*)
        FROM persons
        WHERE name LIKE CONCAT('%', #{keyword}, '%');
    </select>
</mapper>
